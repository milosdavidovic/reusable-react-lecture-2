import React from "react";

/*
  Reuse some logic with hooks (best if comes from project)
  Built in hooks overview?
  useRef vs useState
  useEffect dependency array and return function (eslint autofill)
  Memoization of input callbacks for hooks
  testing hooks?
*/

const Hooks = () => {
  return (
    <div>
      <h1>Hooks</h1>
    </div>
  );
};

export default Hooks;
